package tech.mabunda.card.enums;

/**
 * Enum representing card types in UNO (Number, Action, Wild).
 * <p>
 * Used to distinguish between number, action, and wild cards.
 */
public enum Type {
    NUMBER, ACTION, WILD
}
